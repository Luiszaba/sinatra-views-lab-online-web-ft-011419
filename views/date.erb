<!DOCTYPE html>
<html>

  <body>
    <% time = Date.today %>
    <h1>Today</h1>
    <p>The date is <%= time.strftime("%A") %>, <%= time.strftime("%B") %> <%= time.day %>, <%= time.year %></p>

  </body>

</html>